# tests/ - Unit Test Suite

## Purpose

Hermetic unit tests for Quasarr, built exclusively on the standard-library `unittest` framework. Covers download-link extraction, search-source behavior, search runtime instrumentation and resource-load bounds, the userscript CAPTCHA routing, mirror filtering, download orchestration, notifications, SponsorsHelper helpers, release matching, and the SQLite layer.

## Ownership

`test_*.py` files in this folder and `data/filecrypt_fingerprint_vectors.json`, the synthetic cross-repository fingerprint contract fixture. `test_newznab_publication_dates.py` owns stable publication identity across searches/restarts, concurrent first sightings, RFC-822/ISO canonicalization, non-string and malformed/overflowing source/stored self-healing, UTC request fallback, and the uncapped batch plus hash-only persistence contract. `cli_tester.py` at the repo root is NOT part of this suite - it is a separate interactive/scriptable end-to-end client that exercises a RUNNING Quasarr instance over real HTTP against real configured sources; never confuse the two. `integration_tests/` is a separate top-level boundary with its own AGENTS.md: it needs a second repository on `PYTHONPATH` and is deliberately outside this folder so `discover -s tests` never collects it.

## Local Contracts

- Framework: stdlib `unittest` only - no pytest, no conftest, no coverage tooling, no test config in `pyproject.toml`. Classes are `<Subject>Tests(unittest.TestCase)`, files `test_*.py`, methods `test_*`, each file ends with an `if __name__ == "__main__": unittest.main()` block.
- Full-suite command: `uv run python -X utf8 -m unittest discover -s tests` (the `-X utf8` flag avoids Windows console encoding noise in log output).
- Tests must not perform network I/O or touch JDownloader. Patch in the consuming module's namespace (e.g. `quasarr.downloads.sources.<xx>.requests.Session`), not the `requests` library globally. Only the storage-layer tests (`test_sqlite_database.py`, `test_newznab_publication_dates.py`, `test_crypter_sweep_transitions.py`, `test_crypter_cooldown_statistics.py`, `test_filecrypt_cohort_integration.py`, `test_config_secret_decryption.py`, `test_config_atomic_writes.py`) touch disk, via `tempfile.TemporaryDirectory`.
- Synthetic-data rule (security-critical): source hostnames in tests are fake domains on the reserved `.invalid` TLD; use synthetic release titles (never paste real ones). Real public hoster/crypter domains are permitted only where the production matching logic keys on those literal domains - they are hoster/crypter services, not protected sources.
- `shared_state` is always faked (MagicMock with a `.values` dict, SimpleNamespace, or a small local class whose `values["config"]` is a callable returning dicts) - except the storage-layer tests above, which mutate the real module in `setUp`.
- Tests that run the real `SearchExecutor` with synthetic sources patch `quasarr.search.search_singleflight` to a fresh `SearchSingleFlight()` for each test, preferably from `setUp` with `ExitStack`. Never clear the process-global registry while work is live; leader callbacks own flight removal.
- `data/filecrypt_fingerprint_vectors.json` is the only shared data fixture: it contains synthetic `.invalid` URLs and full expected normalized URL/SHA-256 values, with no real hostname or credential. There is no shared test-helpers module; each file defines its own `FakeResponse`/`FakeSession`/fake shared_state inline.
- Sweep-transition regression coverage spans `test_crypter_sweep_records.py`, `test_crypter_sweep_transitions.py`, and `test_crypter_defer_generations.py`: it pins permanent `inventory_unavailable` taint, plural sorted unique bounded package fingerprints with singular-v2 fail-closed handling, exact 14-field accepted-response history across terminal transitions (the 14th field, `outcome`, must agree with the response it stores), stale unaccepted leases, generation-wide offer-ID history and one-window tested epochs, fresh-versus-duplicate healthy CLEAR behavior, replay side-effect freedom, and rollback for decision, outbox, and 101st-package-fingerprint overflow.
- `test_filecrypt_lifecycle_service.py` covers `FilecryptLifecycleService` opening/leasing/projection: zero/singleton/sweep with exact totals at 2/4/5/101/500/1000/5000; duplicate-owner deduplication; deterministic candidate ordering; excluded-owner membership-preserving bypass; atomic one-mutation opening+first-lease with sweep-header/member/link-state targets; active sweep leases pending then expired-offered members and never newly-added fingerprints; active-held skip, expired-held retest priority, one-first-time-plus-held individual mode; live-healthy first-time suppression with retest pass-through; live-cooldown suppression with valid/invalid/blacklisted probe; expired-cooldown first-time access; all five decision() projections; all six project_link() projections; active_blacklisted_fingerprints() ascending tuple; concurrent opener hook writes no orphan members; mutation exception leaves no partial state; 5,000-member opening via one mutate_values call. Uses copied AtomicDatabase (with mutation_count and rollback), AtomicSharedState, FakeClock, and SequentialIds helpers defined inline. `OfferLeaseDurationTests` pins `OFFER_LEASE_SECONDS` at 300 seconds and equal to its `crypter_sweeps` twin, because a lease shorter than the helper's worst-case browser solve throws a successful solve away as `stale`; `test_crypter_sweep_records.py` pins the same value on the `crypter_sweeps` side, and every test clock offset that has to outlive a lease derives from the constant instead of a literal.
- `test_filecrypt_lifecycle_decisions.py` pins pure report normalization and response validation: exact blocked/access key sets, malformed IDs/bool protocol/wrong terminal operation/extra-missing/bad crypter-reason-access rejected; defer response validation with tested<=total, deadline>0, retry>0, state-pairing coherence (hold+cooldown rejected, sweeping+crypter_cooldown rejected); access response validation with tested<=total, deadline>0; builder produces validated response; receipt privacy scan of serialized responses. Blacklist response validation additions: `BLACKLIST_RESPONSE_KEYS` exact 9-key set, wrong instruction/state/hold_type rejected, any non-zero counter (evidence_count, retry_after_epoch, sweep_tested, sweep_total) rejected, bool counters rejected, zero/bool deadline rejected, bad sweep_id rejected; `build_blacklist_decision(*, sweep_id, sweep_deadline_epoch)` produces exact shape with no extra fields and passes `validate_blacklist_response`; serialized response contains no URL/title/reason/error. 64+ tests.
- `test_filecrypt_lifecycle_outcomes.py` pins `record_blocked`/`record_access`: first sweep BLOCKED creates held at report_now+24h (not offer/sweep deadline); first individual BLOCKED same with no header dependency; BLOCKED does not prevent next frozen member offer; CLEAR/UNKNOWN mark member, create no link state, make global impossible, remaining member continues; 5 all BLOCKED before deadline → cooldown + five holds; 4 all BLOCKED → no holds; 499 BLOCKED + 1 CLEAR → no cooldown; expired lease/wrong offer/deleted protected → stale/no writes; exact replay after member+protected deletion returns same response and no second counters; receipt with conflicting identity/outcome not replayed; callback six targets; injection failure leaves no partial state; receipt raw JSON contains no URL/title/reason; 101-member all-blocked transition; protected value unchanged. Additional coverage: service wrapper returns `{**response, terminal_required: False, fingerprint, package_id, terminal_operation_id}` for fresh and replay; CLEAR response always has `sweep_tested=0, sweep_total=0, state=healthy, cleared=True, accepted=""` with positive deadline, persisted header diverges (remains sweeping); header derivation matrix (None→individual, malformed→stale, live sweeping match→sweep, live sweeping mismatch→stale, live healthy→stale, live cooldown→stale, expired→individual) for both blocked and access; generation binding (report sweep_id != member.generation_id → stale, receipt replay requires generation match); response matrix with tested<=total, positive deadline, coherent state pairings for all six outcome scenarios. `TestBlockThresholdPausesTheCrypter` owns the block-count cooldown trigger: `DEFAULT_SWEEP_BLOCK_THRESHOLD` blocks inside a live window pause the crypter even after an UNKNOWN cleared `global_possible` and while the sweep is still incomplete (header `cooldown`, `retry_after_epoch` = report time + cooldown, cooldowns=1), a paused crypter hands out nothing at all without a queued probe, handouts resume the instant `retry_after_epoch` passes (and not one second earlier), one block below the threshold keeps the sweep running, a window that expired before the report pauses nothing (driven with `filecrypt_sweep_window_minutes=1` so the window dies inside the still-live 300 s lease), and the configured `filecrypt_sweep_block_threshold` is honoured, clamped up to `MINIMUM_GLOBAL_COOLDOWN_SIZE`, and falls back to the default when unreadable. `TestCompleteCohortTriggerSurvives` pins the original trigger alone by lifting the threshold far above the cohort size. Three pre-existing cases (`test_mixed_no_cooldown`, `test_101_members_all_blocked`, and hardening's `test_1000_member_all_blocked_scale`) now lift that same setting above their cohort size, because their subject is the cohort denominator or scale rather than the pause, and at the default threshold the sweep would stop handing out members after the fifth block.
- `test_filecrypt_lifecycle_retests.py` pins retest and probe outcome recording: retest CLEAR deletes held state, writes healthy header with report sweep_id, writes receipt mode=retest/outcome=clear, no counter increment; retest UNKNOWN clears lease and backs `retry_after_epoch` off from report time by one cooldown period while preserving `first_blocked_epoch`, no header change, receipt mode=retest/outcome=unknown, link not immediately re-offerable but retest-eligible again once the backoff elapses; retest BLOCKED transitions held→blacklisting with original first_blocked_epoch and full recheck identity, returns terminal_required=True transition, writes no receipt/header/member/outbox change; blacklisting replay returns same terminal transition even after protected deletion, conflicting identity stale; expired lease/changed owner/mismatched lease stale. Probe BLOCKED keeps held with cleared lease, cooldown defer response, probes+1, receipt mode=probe/outcome=blocked, no blacklisting; probe CLEAR deletes probed hold only, header cooldown→healthy, unrelated holds remain, probes+1; probe UNKNOWN clears lease, header unchanged, cooldown/unknown access response, probes+1; exact receipt replay increments nothing; expired cooldown/wrong lease stale; probe never creates blacklisting. Callback six targets; privacy (no URL/title/reason in blacklisting or receipts); access response validator allows state=cooldown for cleared=False/accepted=unknown only. `TestRetestBlockedDuringLiveCooldown` pins the race where the link's own hold expired but a fresh fully blocked sweep re-armed the sweep-wide cooldown before the report landed: the report always changes durable state (never a no-op), answers a contract-valid `cooldown`/`crypter_cooldown` naming the offer's sweep, clears the lease and backs `retry_after_epoch` off by one cooldown period while preserving `first_blocked_epoch`, leaves header/member/counters untouched, writes a replayable `retest`/`blocked` receipt, and - driven round after round - retires the link into `blacklisting` within `MAXIMUM_HELD_SECONDS_BEFORE_BLACKLIST`, so a permanently blocked link cannot be offered without bound. `TestRetestUnknownLoopIsBounded` pins the same property for a link that can never prove access either way: a tight loop of UNKNOWN reports with no elapsed time yields zero further offers, and offers only resume once a full cooldown period has elapsed, so a helper stuck reporting `unknown` forever cannot be re-offered without bound. `TestRetestUnknownDoesNotResetTheRetirementClock` pins that an interleaved UNKNOWN retest never rewrites `first_blocked_epoch`, so a link alternating UNKNOWN and BLOCKED-during-live-cooldown retests still reaches `blacklisting` measured from its original first block.
- `test_filecrypt_lifecycle_blacklist_confirmation.py` pins `FilecryptLifecycleService.confirm_blacklist()`: fresh exact blacklisting row → blacklisted link-state + receipt + wrapper; preserves first_blocked_epoch and sets blacklisted_epoch at confirmation clock; receipt carries stored package/sweep/fingerprint identity and exact blacklist response (mode=retest, outcome=blocked); exact replay after link-state is blacklisted returns byte/equality-identical wrapper and changes nothing; wrong offer_id or terminal_operation_id returns None with no writes; invalid argument syntax (not 64-hex fingerprint, not 32-hex offer_id, not 64-hex terminal_operation_id) raises ValueError before any storage call; absent link state or held/blacklisted-without-receipt fail closed; malformed link state fails closed; malformed or conflicting receipt (wrong outcome, wrong mode, wrong fingerprint, even with valid blacklisting) fails closed; only two mutation targets (filecrypt_link_states, filecrypt_offer_receipts), no header/member/protected/outbox touched; injected second-target write failure rolls back link state and receipt; concurrent exact calls produce one committed receipt and identical results (deterministic before_mutation hook); privacy scan: raw link/receipt JSON contains no URL/title/reason/error. 51 tests.
- `test_filecrypt_lifecycle_hardening.py` pins `FilecryptLifecycleService.prune_receipts()` and lifecycle storage resilience. Pruning: empty table → 0/no mutation; `expires_epoch == now` removed, future retained; malformed row preserved while expired valid removed, count-only warning has no identifier (no key/fingerprint/package_id in message); advisory warning at `RECEIPT_ADVISORY_THRESHOLD=4096`, absent below it, operation continues; 5,000 expired pruned in exactly one `mutate_values` call; 5,000 live → 0/no mutation; concurrent replacement via `before_mutation` survives exact-compare while other expired rows are pruned; write failure propagates with rows unchanged; clock called exactly once before callback (raises on second call). Hardening (fake store): CLEAR committed inside BLOCKED's `before_mutation` hook → BLOCKED returns None due to conflicting receipt, sweep ends healthy, no held row for last member, outbox observations=4/cooldowns=0 (5 members, 4 BLOCKED + 1 CLEAR); 1,000-member all-BLOCKED sweep → cooldown with sweep_tested=1000/sweep_total=1000, 1,000 held link states, 1,000 receipts, outbox observations=1000/cooldowns=1. Hardening (real SQLite): duplicate BLOCKED from two threads after Barrier → both return non-None equal wrappers, one receipt, header tested=1/blocked=1, one held row, outbox observations=1; injected `_upsert_value` failure after ≥3 writes during record_blocked → exception propagates, all 6 raw target values byte-identical from independent reader connections. 14 tests.
- `test_crypter_candidates.py` pins SponsorsHelper-compatible credential/port/fragment/query normalization, full SHA-256 fingerprints, canonical enabled Filecrypt-only inventory, package-ID/link-index ordering, global fingerprint deduplication with every occurrence retained, frozen non-JSON-serializable records, and both fail-closed capacity sentinels: unique fingerprint 101 and occurrence 1001. The occurrence case deliberately repeats an already-known fingerprint, so only the occurrence bound can explain the sentinel, and a guarded resolver fails the test if any later link is inspected. It also pins that the inventory admits exactly what `helper_package_is_candidate()` admits. `FilecryptLifecycleInventoryTests` pins that `enumerate_filecrypt_lifecycle_candidates()` never returns `oversized=True`, handles 101/1000/5000 unique fingerprints and multi-package duplicate owners, and returns an empty non-oversized inventory for zero rows. Its import lives in `setUpClass` so it cannot break the bounded tests if the function is missing.
- `test_filecrypt_lifecycle_records.py` pins the pure lifecycle record codecs against the approved schema: table-name and key constants, `MINIMUM_SWEEP_SIZE=2`, `MINIMUM_GLOBAL_COOLDOWN_SIZE=5`, exact key sets and round-trips for all five record types (link-state, sweep-header, sweep-member, offer-receipt, migration-marker) across every legal state/mode/outcome, rejection of each missing key, each extra key, wrong `schema_version` values (including `True` which would pass `==1` without the `type(v) is int` guard), booleans as epoch integers, integers as `global_possible`, sweep invariants (`tested <= total`, `blocked <= tested`), the `cooldown` state's strictly-positive `sweep_deadline_epoch` (epoch 0 rejected by both encoder and decoder), terminal member states requiring `outcome`, non-terminal states requiring `outcome=null`, bad 32/64-hex identifiers, non-canonical package IDs, non-dict `response`, invalid `mode`/`outcome` enums, malformed JSON, `None` input, JSON arrays/nulls, 100 000-deep nesting (RecursionError), 5000-digit integer literals (ValueError), oversized records, and lone surrogates. Encoder tests pin `TypeError` for non-dicts and `ValueError` for every schema violation. Predicate tests pin `link_state_is_held` and `link_state_is_recheck_eligible` at, before, and after `retry_after_epoch`, for non-held states, and mutual exclusivity across the deadline boundary.
- `test_crypter_sweep_records.py` pins the version-two decision codec against the approved spec: the constants, each state's exact key set (every single-key removal and every extra key is rejected, on both encode and decode), the member and live-offer key sets, 32/64-character lowercase-hex ID and fingerprint patterns, ascending unique members and retest members, the cohort bounds on both ends with `cohort_size` agreeing with the frozen list, closed result/instruction/mode/reason enums, non-negative non-boolean epochs, the exact one-window sweep deadline, and deterministic sorted URL-free round-trips. A cohort cooldown additionally requires complete terminal evidence: every member is `blocked`, has coherent tested/offer fields, replays only `hold`/`cooldown`, and exactly one member owns the `cooldown` response; pending, offered, unknown, or malformed members never project `cohort_size` as evidence. Robustness is proven by an unreadable-row table (bad JSON, wrong types, a lone surrogate, a 5000-digit integer literal that raises a plain `ValueError`, a 100,000-deep nesting built by string repetition that raises `RecursionError`, and schema versions 1/3/`2.0`/`True`), and the byte limit is proven by patching `MAXIMUM_COHORT_RECORD_BYTES` to exactly the encoded length and to one byte less. Encode-side surrogate sizing is forced at the JSON boundary to prove it raises a controlled `ValueError`/`OverflowError`, never raw `UnicodeEncodeError`. Expiry is pinned at deadline minus one, deadline, and deadline plus one for every state, including that an expired sweep is never decoded away. Migration is pinned to drop an old `observing` row at 1-4 observations, to preserve an old cooldown's exact retry deadline and surviving evidence count with a floor of one, to invent no cohort field, and to permit additive fields only on mappings without `schema_version`; v2/future hybrids never fall back to legacy. The service tests use a mutation-counting fake database with a deterministic before-mutation hook to prove that a valid version-two row projects into the existing snapshot keys with zero writes and a byte-identical row, that a legacy `observing` row keeps its current snapshot and is never promoted, that unsupported versioned hybrids self-heal instead of becoming active cooldowns, and that cleanup preserves/projects a concurrently installed valid v2 row after an invalid, expired-legacy, or future-version initial read. Self-heal-on-read is table-driven and splits the two cases deliberately: the four genuinely unreadable rows each warn exactly once, while a structurally valid record whose own window merely ended is deleted silently, because a clean expiry is not a discarded row.
- `test_crypter_sweep_transitions.py` pins the version-two transitions and their atomic wrappers. The pure half is table-driven over cohort sizes 0/1/2/4/5/100/101, a deadline pinned at minus one, exactly, and plus one for every state, an `UNKNOWN` at every one of the five member positions, duplicate replay, superseded offers and wrong generations, blocked reports during healthy/individual, inventory disappearance of a tested versus an untested member, late inventory additions, an unusable inventory, offer-ID collision, and the version-one precedence table; every record a transition emits is round-tripped through the strict codec, which is what proves the cohort cooldown's evidence coherent. The wrapper half runs against a fake database that fails the test if any storage read happens inside a mutation and against a shared state that fails if a mutation callback resolves storage at all, which together pin that targets and settings are resolved before the transaction. `mutations` records the exact `(table, key)` tuple list, so the single three-row `mutate_values` transaction is asserted structurally rather than by effect. Rollback is proven twice - a ledger seeded to the count ceiling, and a `MAXIMUM_COHORT_RECORD_BYTES` set just large enough to decode the frozen sweep but too small for the row the accepted report would write. Concurrency uses deterministic seams only: a `before_write` hook commits a competing CLEAR immediately before the fake transaction opens, and against real SQLite a second connection commits one through a patched `DataBase.mutate_values`; both leave `healthy` current and produce no cooldown. Real SQLite additionally proves the committed cohort, its holds, and its ledger survive reopening, and that two threads never lose a member result. Because real fingerprints are hash-ordered, every real-database test resolves the offered member's owning package from the inventory instead of assuming one.
- `test_crypter_defer_generations.py` pins the unchanged six-key legacy shape and exact nine-key version-two shape with `schema_version=2`, a 32-lowercase-hex sweep ID, and 1-100 ascending unique 64-lowercase-hex `link_fingerprints`; every missing/extra/wrongly typed field is rejected, the transient singular-v2 shape authorizes nothing, and no defer block carries a raw link. Activation covers every decision state, including fingerprint intersection for `inventory_unavailable`; writer coverage proves same-generation merge and fail-closed overflow before fingerprint 101. Compare-and-clear and cohort cleanup retain their deterministic fake/real-SQLite race tests, one-read projection tests, malformed-row table, per-target failure continuation, sanitized reasons, and `KeyboardInterrupt` propagation.
- `test_crypter_cooldowns.py` pins the canonical protected-crypter resolver, import isolation, fixed three-distinct-observations/15-minute evidence rule, immutable duplicate evidence and provisional retry deadlines, first-surviving observation semantics, minimum cooldown, additive/malformed persistence handling across every service API, sanitized warnings, read-only snapshots, no-URL schema, state validation, and successful-access clearing. Those rules describe rows carrying no version-two decision, which is exactly the state a version-one caller still meets; `test_crypter_sweep_transitions.py` owns the collision case where a version-two row is present. `test_deferred_protected_packages.py` pins the protected-package `deferred` schema, field preservation, the single provisional hold budget, the carried-forward generation binding, read-only defer reads, the dynamic queue projection and status prefix, and database-only batch deletion including real-SQLite duplicate-row interleaving, per-ID verification, and zero JDownloader calls. Its `LifecycleHeldDeferredProjectionTests` pins the `filecrypt_link_lifecycle_v1` fallback: a `held`/`blacklisting` lifecycle link state (written directly via `filecrypt_lifecycle.encode_link_state()`) projects onto its owning package's queue item with the link's own `state`/`retry_after_epoch`/`since_epoch`, a package carrying an active legacy hold keeps projecting through that path byte-for-byte unchanged even while its own link is simultaneously lifecycle-held, an expired/inactive legacy hold falls through to a live lifecycle hold instead of reverting to the ordinary queue, and a package with neither is untouched. `test_sqlite_database.py` separately proves real-SQLite read/write traces, deterministic concurrent mutation ordering, begin/callback/type rollback, committed delete/upsert semantics, exact one-row deletion, nested Config/DataBase rejection, and - through the connection trace callback - that one cohort transition is a single `BEGIN`/`COMMIT` inserting into `crypter_cooldowns`, `protected`, and `crypter_events` in that order, with a ledger overflow leaving all three untouched on disk.
- `test_package_origin.py` pins the durable package-origin record: host-only `safe_mirror()` (a scheme, path, query, credential, space, empty port, or >253 characters all read as ""), the crypter label map with a readable fallback for an unknown key, write-once `record_package_origin()` (a second call with a different crypter returns False and leaves crypter/mirror/`added_epoch` untouched), rejection of an empty package id or a non `[a-z0-9_]` crypter key, credentials stripped from a URL before the host is stored, and read-side sanitizing - a malformed row is skipped, a stored URL in `mirror` reads back as "", a negative epoch reads as 0.
- `test_crypter_block_settings.py` pins settings defaults/persistence, refresh-before-cache ordering, malformed-input preservation, authenticated route registration, the rendered Link Protection controls and `quasarrApiFetch` save boundary, the exact non-destructive fail-mode `/defer/` response, and the runtime block-mode gate itself: against real persisted cooldown plus package defer metadata it asserts that `fail` restores legacy capable selection, same-package link alternatives, and the plain CAPTCHA queue projection without building a `CrypterCooldownService`, that exclusions still apply, that unknown/missing modes defer, and that flipping the cached key in either direction changes behavior without touching the stored rows. `CohortBlockModeTests` extends that gate to the cohort path: `fail` hands out no offer and writes no decision row at all, a cohort `/defer/` report answers the cohort-shaped bypass without a single `get_db` call, and flipping back to `defer` opens the sweep. It also pins that the capability - not the cooldown service - drives the canonical package ID contract: in `fail` mode a capable helper skips a non-canonical row (which no exclusion list can name) and receives the next valid package without any cooldown read, honors alphanumeric custom-category exclusions, while an incapable helper still receives that same row. `CrypterBlockSettingTests` additionally pins sweep-window precedence (stored override > ENV > default 15, invalid ENV logs once without the raw value, invalid stored falls through silently), save/clear/null semantics (valid int persists, null deletes the stored override and resolves ENV/default, invalid value preserves current, cache updates come after all DB operations), and that an active generation's `deadline_epoch` and `window_seconds` are frozen at open time and unchanged by a subsequent settings update. Because `get_api()` installs its dict as the process-global shared state, `DashboardControlsTests` restores the previous `shared_state` values/lock.
- `test_deferred_packages_api.py` pins the Packages page deferred section and its commands: only queue rows whose projected `deferred.active` is true leave the ordinary queue, cards expose linkcrypter/reason/state/evidence/countdown/probe labels plus the manual CAPTCHA link but never a URL, hostname, sweep ID, or link fingerprint, every dynamic value in the deferred, queue, and history renderers is escaped, the AJAX fragment carries the same section, and the authenticated `POST /api/packages/deferred/probe` / `DELETE /api/packages/deferred` routes reject a non-list payload before delegation and otherwise return the atomic helpers' exact ordered per-ID results against real in-memory protected rows with zero JDownloader calls, for generation-bound holds exactly as for legacy ones. It also pins that `fail` block mode makes probe inert (exact response, no `CrypterCooldownService`, byte-identical persisted defer metadata) while deletion still removes the row, and that the background refresh preserves the deferred selection: the snapshot/replace/restore order is extracted from the shipped `refreshContent()` by brace matching and replayed against a checkbox model parsed out of two real rendered fragments, so a missing, reordered, or retaining implementation fails on the resulting selection rather than on a string match.
  There is no JavaScript engine in the suite, so nothing here executes JS: that replay drives a Python checkbox model, and the shipped `restoreDeferredSelection` is pinned separately by `restore_selection_contract()`, which extracts the helper and requires exactly one parameter, `new Set(<that parameter>)`, a quoted-literal selector, a `<set>.has(<checkbox>.<prop>)`-guarded `checked = true` whose `<prop>` is the property `selectedDeferredPackageIds()` itself reads, exactly one `.checked =` write, and set/iteration/guard ordering. Eleven mutations of the shipped helper text prove those rules: the original eight cover empty/constant `Set`, a parameter disconnected from the set, `has(packageIds)`, wrong property, built selector, and unguarded or extra unguarded restore; three message-targeted mutants cover parameter count, `checked = true`, and operation order.
  Selector-injection safety covers both `querySelector` and `querySelectorAll`, reads each argument with quote and paren awareness, and requires every argument to be a quoted literal. It audits the static page, a real value-bearing deferred/ordinary/history fragment, and the composed page; a selector-hostile package ID is present in the rendered fragment but absent from every selector argument.
- `test_crypter_cooldown_statistics.py` drives the real routes, the real `CrypterCooldownService`, and a real SQLite file (one connection per table in a `TemporaryDirectory`) to pin the counter semantics: one observation per newly recorded distinct evidence and none for a duplicate package/fingerprint, exactly one `crypter_cooldowns` per entry into cooldown (including a second one after expiry, and none for further reports or a repeated blocked probe while cooling), and `crypter_probes` only where a queued probe was actually spent. The `deferred_packages` gauge is asserted as a live count over protected rows across hold, expiry, CLEAR, delete, repeated clears, and `fail` mode (which also leaves both stores byte-identical), and it is never a persisted row. Failure counters, `fail()`, and notifications are asserted untouched, and `build_solved_data` is pinned to the `name` key.
- The same file's `CohortSweepCounterTests` drives the version-two writers over the real inventory enumerator and the same real SQLite file to pin the redefined counters: one observation per newly blocked cohort member and none for a duplicate or stale report, exactly one `crypter_cooldowns` for a complete all-blocked cohort and none for a cohort below the conclusive minimum, no counter movement at all from `UNKNOWN`, `CLEAR`, or a leased cohort probe (which is not a consumed package probe), the `deferred_packages` gauge following cohort holds and dropping to zero on CLEAR, and a ledger at the count ceiling rolling the whole cohort transition back with the decision row and the package row untouched.
- Since a validated version-one CLEAR became the global healthy transition, that file's legacy CLEAR cases assert the stored row is a `healthy` version-two decision rather than an absent one, and that the health window drops the `deferred_packages` gauge to zero at once; only when it expires do the untouched legacy rows fall back to their own stored deadlines.
- Statistics outages are injected at the table handle (`RealDatabaseSharedState.statistics_unavailable()`, optionally writes-only) instead of being mocked away, which is what pins that a failing statistics table changes no defer response, no hold, no handout, and no probe, that the `crypter_events` ledger keeps the exact counts across the outage, a retry, a restart, and CLEAR, and that a read reports pending events without ever counting them twice. Two discriminators back this up: `rejecting_single_row_writes_to()` fails the test if any transition is recorded by a second `mutate_value` call outside its deciding transaction, and patching only `DataBase.retrieve` proves the flush is transactional - a competitor commits between a read and a write, so a read-then-write flush loses it while the real flush never calls `retrieve` for that key.
- Concurrency in the linkcrypter statistics is exercised against real SQLite with storage-seam hooks, not by mocking the unit: `flushing_from_another_connection_before()` runs a complete flush from a second connection just before a counter row is read (a two-read total then counts the same event twice), `flushing_from_another_connection_before_the_snapshot()` does it just before the snapshot transaction, `recording_single_row_reads()` fails if the ledger or any counter is read outside that snapshot at all, and `replacing_the_ledger_once()` commits a valid ledger row from another connection right before a cleanup, so an unconditional delete erases it. `recording_single_row_writes()` pins that an empty ledger keeps the statistics page write-free. Ledger robustness is table-driven over `MALFORMED_LEDGERS` (invalid JSON, non-objects, wrong keys, booleans, negatives, floats, strings, a 5000-digit integer literal that raises a plain `ValueError`, and a 100,000-deep nesting - built by string repetition so the fixture never recurses itself - that raises `RecursionError`, pinned as such by its own assertion so a changed interpreter fails there rather than silently weakening the self-heal proof), and `LEDGER_COUNT_CEILING` pins that counts stay exact far above 1,000,000 while a count that no longer fits fails the whole transition and leaves both rows untouched.
- `test_filecrypt_cohort_api.py` owns the cohort HTTP surface. Its pure half pins both-capability gating, the terminal operation digest, a URL-free rendered offer that must match its occurrence, strict offer-identity normalization (case, length, missing field, non-Filecrypt crypter), tri-state classification of both report routes, and the exact cardinality of every rendered body. Its route half drives the real `CrypterCooldownService` over an atomic fake store with an injected clock and sequential identities: exact first-occurrence handouts with no second Filecrypt URL, a tested link never hiding a pending link of the same package, every occurrence of a tested fingerprint staying blocked, retained alternative linkcrypters, the shared candidate predicate deciding membership, the oversized inventory refusing a partial cohort and reporting the `101` sentinel on a stale report, an unreadable inventory that can never cool, ascending retest order, a consumed manual probe, the whole `/defer/` matrix, HTTP-200 defer `stale` versus HTTP-409 access `stale`, duplicate replay, exact six-field version-one bodies under every version-two state, a body naming no offer falling back without touching version-two state, the version-one CLEAR health transition with idempotent retry and cleanup-failure tolerance, `fail`-mode purity on every route, and that no route log carries a URL, fingerprint, or offer ID. `LifecycleRouteTests` also owns the owed terminal blacklist on `/defer/`: a `blacklisting` link state plus a persisted operation record is the whole fixture, and the report a helper still owes is admitted and delivered when the identity it names is bound to a `complete` record of another outcome whose package is protected again, refused 409 while an operation is really in flight under another outcome, and refused 409 when the protected store cannot be read. Its store models the removal and the operator message only for the span of that report, because the module's fake deletes no rows. `RealDatabaseCohortTests` repeats the full cohort and CLEAR flows against a real SQLite file. Because real fingerprints are hash-ordered, every test resolves the offered member's owning package from the inventory or drives the sweep until the member it needs is offered, instead of assuming a slot.
- Regression coverage for the cohort HTTP surface lives in the same file plus `test_crypter_sweep_transitions.py`. `MalformedCohortIntentTests` drives a table of broken cohort identities through both routes and asserts HTTP 400 with the decision row and every protected row byte-identical, that malformed intent never opens a legacy observation, and that a body with no cohort field still takes version one. `OfferOccurrenceBindingTests` pins the stored-index binding with two raw URLs of one package that normalize to one fingerprint, a Filecrypt link at a non-zero index, and a fingerprint shared by two packages. `ManualProbeTargetingTests` pins that a probe offers its own queued package (deliberately choosing a package that is not the cohort head), the lowest covered stored index of a multi-member package, and that a `crypter_defer_v1`-only handout can never spend one. `OfferOwnershipTests` covers a foreign, arbitrary canonical, deleted, and relinked reporting package plus both inventory-outage directions. `ProvenOwnershipTests` is the tri-state matrix: a proven owner still records, a readable inventory or a readable row disproves a foreign reporter (every row of every table byte-identical, including the expired cooldown a transition would have pruned), a deleted, malformed, or unreadable row and a storage error leave ownership unknown so the blind taint still applies with no package row written, an ineligible row that still carries the link is unknown rather than a mismatch, a CLEAR that cannot prove ownership is HTTP 409 stale rather than healthy or tainted, an UNKNOWN access is still accepted while unknown, the storage error never reaches the response or the warning, and an offer becomes replayable only after an accepted report from a proven owner. Its real-SQLite twins repeat the foreign and unprovable CLEAR against a real file. `VersionOnePrecedenceRaceTests` and the real-SQLite twin install a sweep through a transaction-boundary hook, and `LegacyClearOrderingTests` pins commit-before-release, contained cleanup failures, a preserved newer generation row, and physical release of a generation-less legacy hold. `PreferredMemberLeaseTests` and `TypedReplayTests` pin the pure halves: a named lease that mints nothing when impossible, the stored outcome kind, cross-route replay staleness, and counters recomputed from the current record while the verdict is preserved. Its service-level outage pair pins both directions of a blind report: a reporting row that cannot be read still ends the generation fail closed and never gets a package row back, while a readable one that lacks the reported link is stale and opens no transaction at all.
- `test_sponsors_helper_api.py`'s `CoherentDeferProjectionTests` drives the real service to pin the boundary obligation that capable selection passes the coherent `crypter_projection()` decision: a generation-bound hold really suppresses its own link there, and only that link - the same package's alternative linkcrypter is still handed out. Its `FakeCooldownService` therefore exposes `crypter_projection()` and the three-argument `project_package_defer()`. Its two legacy CLEAR ordering cases inject the failure at a single table handle: a protected-store outage still acknowledges proven health, while a linkcrypter-store outage answers HTTP 500 with the hold intact.
- `test_terminal_operations.py` owns the exact terminal-operation digest/codec and service invariants: total malformed decoding, secret-free exact keys, validation before writes, prepared/submitted/complete replay, conflict, capacity including two concurrent IDs competing for the last slot, retention boundary, deterministic compare-delete pruning including the oldest-first deletion order, fixed-cardinality counts, restart durability, and real-SQLite concurrent replacement preservation. The phase cases pin the ordered `(state, effect_state)` pairs, reject every incoherent pair, migrate the exact seven-key legacy row (`prepared` -> `not_started`, `submitted`/`complete` -> `applied`) and the eight-key one, prove neither claims bookkeeping it never wrote, prove only a seven-key row past `prepared` is `legacy_unproven`, prove that provenance is never persisted, and prove the row is rewritten in the current shape at its next transition; `legacy_record()` is the seven-key fixture, `effect_record()` the eight-key one and `record()` the current one. `TerminalFailureBookkeepingTests` pins the failure ledger: the operation row, the failed row and both counters are ONE recorded transaction over exactly those targets, a commit that never lands leaves all of them untouched, a repeat writes and counts nothing, an unusable counter row counts from zero, nothing may be persisted before the attempt is durable or for another report, adopting an older shape's row invents no counter, and the notification phase only moves forward. `operation_evidence` is pinned as the documented digest, stable across transitions, different for a later lifecycle a whole retention window on, and free of any readable identifier, and the comment cases pin the marked/unmarked round trip plus every malformed marker. Its guarded fake fails any storage lookup from a mutation callback. Retention is pinned on ordinary begins far below capacity, at the exact boundary, against active rows, and across a re-added package that may open another terminal state only once its expired record is gone. `RetiredOtherOutcomeTests` pins the same retirement under `completed_state`: a complete record of another outcome is replaced by the exact freshly prepared row of the requested one, opening strictly later so their evidence differs, while a record short of `complete` resumes with no write, a record naming a third outcome conflicts, and a `completed_state` outside `TERMINAL_STATES` raises before any write. `ReopenedTerminalOperationTests` pins `reopen_completed()`: a complete row is replaced by the exact freshly prepared row carrying none of its bookkeeping, the replacement opens strictly later than the row it retired so their evidence differs inside one second of the clock, every phase before `complete` resumes with no write, a missing, foreign, differently-stated or unreadable row conflicts with no write, a malformed identity raises before any write, and the replacement then runs the ordinary progression as one row of the same identity. The lock cases prove the per-operation path is a digest under the temp directory, distinct per operation, not the admission lock, held across admission and the transition, released on failure, never taken for a malformed identity, and that a waiting thread reads only what the holder left behind; cross-process exclusion itself stays owned by `test_storage_lock_fork_safety.py`.
- `test_sponsors_helper_terminal_confirmation.py` owns the three terminal routes. It pins the byte-compatible legacy bodies; one uniform exact v2 envelope and 400/409/503 mapping; structured HTTP-200 retry bodies; JDownloader reconciliation after a lost submitted write with exactly one `add_links`; cleanup and completion crash windows; complete replay; and exact-once failed history, notifications, download/decryption counters, and disable state. A structured success is impossible while the protected package is still present or the disabled flag is unproven. Concurrency is deterministic and needs no sleep: `SideEffectGate` parks the first request inside the one irreversible step (`add_links`, `commit_terminal_failure()`, the protected disable write) and a second identical request is started behind it, so a duplicate transition sets the gate's second event and is asserted directly; a serialized one can never reach it. Provenance is covered per route by `seed_operation(..., shape=)`, which persists the record a crashed request would have left (`not_started`, `attempting`, the eight-key row or the seven-key legacy row) and returns its evidence: a crash before the effect must still submit/fail/disable next to a stale unmarked failed row, JDownloader package or disabled flag, and a crash after it may only reconcile from the artifact carrying that evidence. `TerminalFailurePersistenceTests` pins that nothing observable precedes the failure commit - an unwritable history table produces no counter, notification or removal on all three failure branches and still resumes later - plus the write/notify/remove order, the pending-phase-before-dispatch order, and the three crash windows (after the row, after dispatch before its marker, after everything before `mark_submitted`), each proving no duplicate and no silent success. `MigratedTerminalRecordTests` pins that a seven-key `submitted` row does nothing at all while its protected package exists, completes with `package_removed=True` and no side effect once it is gone, is never solved by an unmarked failed row, JDownloader package or disabled flag, replays a `complete` row identically while that package stays gone and answers nothing at all once it is back, still runs a `prepared` row's own transition, and does not block the eight-key shape, whose markers still prove it. `ReGrabbedPackageTerminalTests` pins the same discriminator for current-shape complete records on all three routes: a repeated request replays for as long as the package is gone, and a package that is protected again retires the closed record and runs a second life that submits, fails or disables under new evidence, is removed or re-disabled, counts and notifies once more, and then replays on its own terms; a disable whose own marker is still on its package replays instead of disabling twice, a protected table that cannot be read decides nothing and leaves the record byte-identical, and two concurrent requests for a re-grabbed package still submit exactly once. `ReboundTerminalOutcomeTests` pins the admission path of the same discriminator: a package protected again after a complete download is admitted for a failure and records it once under new evidence, then replays on its own terms, while a package that never came back, an operation still in flight under another outcome, a package still carrying its own disable, a record that marked no artifact, and a protected store that cannot be read all keep answering 409 with the record byte-identical; two concurrent reports for a re-grabbed package retire and fail it exactly once. `crash_after(ordinal)` loses the n-th operation-table transaction; for a v2 failure those are `begin`, the durable attempt, the two notification phases, `mark_submitted`, `mark_complete`, with the atomic failure commit as the one `mutate_values` between the second and third. `MemoryTable.events` records access order, which is what distinguishes "decided from history" from "verified its own write" when the two answers otherwise agree, and `MemoryTable` reaches its peer tables the way one SQLite connection does so a multi-table transaction is really one commit.
- Cohort-projection coverage spans `test_crypter_cooldown_statistics.py` and `test_deferred_packages_api.py`: statistics expose only fixed sweep/progress/deadline/cooldown/retest/individual and terminal-state fields, while deferred cards render sanitized tested/total/deadline/retest values only for a projected cohort and never expose a sweep ID, operation ID, fingerprint, URL, or hostname. Hostile projection values are rejected before interpolation. The countdown contract is the same static-then-replay pattern as the selection restore: `countdown_epoch_contract()` requires exactly one `dataset.cohortDeadlineEpoch ?? dataset.retryAfterEpoch` read, on one element binding, inside a one-parameter helper the updater calls without reading a dataset itself, and each shipped-text mutant of that read is asserted to be rejected. The replay parses the real rendered elements and re-evaluates the same fallback over several ticks, so a cohort card that carries only its own deadline is proven to keep counting down.
- `test_filecrypt_cohort_integration.py` owns the composed cohort behavior: it drives the shipped SponsorsHelper handout/report/terminal callbacks and the operator probe route against a real SQLite file, with only the clock, the identity factory, and the JDownloader device doubled. It pins the denominators that may and may not cool (1, 4, 5, and the 101 oversized sentinel), a complete all-blocked sweep and its per-answer counters, a CLEAR at every one of the five member positions with the exact ascending retest queue it owes, two Filecrypt links of one package as two members, one fingerprint in two packages as one member with both occurrences held, a retained alternative linkcrypter, a late member that never joins the frozen cohort and a deleted untested one that ends it inconclusive, UNKNOWN as non-evidence, an operator probe and the CLEAR it can produce, a CLEAR committed inside the final BLOCKED transaction, version-one-only selection, `fail`-mode purity, and the terminal confirmation of a cleared member. Discrimination was proven with eight ephemeral production mutants (premature cooldown, no retest queue, UNKNOWN counted as blocked, dropped duplicate occurrence, removed terminal replay, `fail` mode no longer bypassing, an unauthorized probe, and a transition decided from a pre-transaction read) in a throwaway copy of the worktree, never in it.
- `test_filecrypt_lifecycle_blacklist.py` owns the owner-scrub route integration: `blacklisted_owners` / `remove_blacklisted_link` service methods and the `scrub_blacklisted_owners` route helper against real SQLite. Covers scrub with/without alternatives, CAPACITY/CONFLICT/UNREADABLE outcomes, absent reporting package idempotence, and partial failure resumption.
- `test_sqlite_database.py` additionally pins that an existing table gains exactly one non-unique key index without losing duplicate rows, while an already indexed table opens without another schema commit.
- Run the full suite after touching shared providers, download flow, search behavior, or notification logic. Per root change discipline, tests change only when the intended behavior in the covered area changed or the existing test is incorrect.
- `test_search_runtime.py` covers `quasarr/search/runtime.py`: it builds `SearchRuntime` with an injected clock and memory reader (never the module singleton's real ones), and asserts the snapshot key set exactly, so a counter carrying a source initial, query, or category ID fails the suite. The `/proc` reader is driven through `patch("quasarr.search.runtime.open", ...)` with in-memory file content, so no test reads a real `/proc` path or any other file.
- `test_search_resource_load.py` is the offline burst gate. Its single `_run_burst(config)` drives the real Newznab category planner, source eligibility, `SearchExecutor`, bounded cache, process-global singleflight shape, worker budget, and overdue callback with synthetic doubles and `.invalid` URLs; events force overlap and timeout, and every wait/join has a five-second failure bound instead of a sleep. The burst also writes 2,049 minimal entries through `SearchCache.set()`: production must evict to at most 2,048 entries and 50,000 releases, while the deliberately unbounded configuration must behaviorally retain more than the production entry bound. The timeout control records the real drop/overdue counters and samples `overdue_drained.is_set()` at the grace measurement before cleanup; production must have drained, while the deadline-ignoring configuration must still hold its overdue token. Production defaults must yield no `_bound_violations(...)`; the permanent unbounded/no-budget/deadline-ignoring configuration must yield behavioral cache, budget, and overdue violations and then drain every harness-owned thread during test cleanup. The test never uses network I/O, real source values, the live deployment, or `IdleMemoryReclaimer`.
- UI-preference and page-dispatch focused contracts live in `test_ui_preference.py` and `test_page_dispatch.py`: strict mode validation, one-write/one-cache persistence, malformed JSON 400 shape, real `add_auth_hook` API-key enforcement, browser cookie/redirect sanitization, startup boot ordering for preference load, Carbon/Classic selection precedence, HTTP control-flow passthrough, and real Bottle response snapshot restoration (status/content-type/duplicate headers) on Carbon fallback. `test_ui_preference.py::test_boot_reconfigures_stdout_to_utf8_before_first_print` additionally pins that `run()` reconfigures `sys.stdout` to UTF-8 before its first print, using a fake stream that raises `UnicodeEncodeError` exactly as a real cp1252 console does until reconfigured - a regression guard for a real crash on the frozen PyInstaller standalone build (and any non-UTF-8-locale console). `DEFAULT_UI` is `"carbon"`; every assertion in `test_ui_preference.py` that pins the literal fallback value (the constant itself, `get_active_ui()`'s all-invalid case, and `load_ui_preference()`'s invalid-value/storage-failure cases) expects `"carbon"` accordingly. Two unrelated suites - `test_deferred_packages_api.py`'s `render_packages_page()` helper and `test_captcha_userscript_routing.py`'s whole `CaptchaUserscriptRoutingTests` class - render real routes to assert Classic-only inline-script/markup contracts that predate Carbon UI entirely; both now pin `QUASARR_UI=classic` explicitly instead of riding the implicit default, so their actual intent (Classic's own contract) stays independent of whichever mode `DEFAULT_UI` selects.
- Carbon shell/component focused contracts live in `test_carbon_templates.py` and `test_carbon_csp.py`: the `TableColumn`/`data_table` table primitives (still public and tested, but unused by any shipped page since the Dashboard and Statistics rebuild) + component API surface, shell landmarks/controls/nav state, strict escaping boundaries, auth-safe API-key meta handling, closed token validation, structural HTML guards (inline handlers/scripts, remote resources, duplicate IDs, unlabeled icon buttons, unsafe `target="_blank"` anchors), no renderer-owned emoji, CSS/JS structural contracts (theme init before DOMContentLoaded wiring, breakpoints, reduced-motion, token blocks, no remote URLs), and exact Carbon-only CSP application via `render_page` while Classic fallback stays byte-identical and CSP-free. `test_carbon_templates.py` also pins `carbon_templates.protected_captcha_count()` directly (Carbon UI integration hoist out of the private per-view helper it replaced): counts titles from the `protected` DB, treats an empty/falsy result as 0, and on a read failure returns 0 without raising while logging only the exception class name (never its text, which could carry a hostname) - plus a `.cds-kpi-row` CSS layout contract (4-column `repeat(4, minmax(0, 1fr))` grid at a 1px hairline gap, collapsing to 2 columns at the existing 1056px breakpoint and 1 column at the existing 672px breakpoint) proven with `assertRegex`/`assertNotRegex` sweeps of the raw `carbon.css` text - a second CSS-regex pin technique alongside the brace-balance guard above, this one checking property values rather than structure: exact dimensions on `.cds-status__dot`, the column template on `.cds-grid--dashboard`, the dense `columns: 380px` layout plus per-tile `break-inside: avoid`/16px margin on `.cds-grid--settings` (packs short tiles under short tiles instead of a fixed-row grid leaving dead space) and its single-column collapse at the 1056px breakpoint, sizing on `.cds-modal__actions .cds-btn`, and that `.cds-tile` itself carries no border (spacing/gap alone separates tiles). A separate pin in the same file proves `.cds-grid--2`'s two fields subgrid across shared label/input row tracks, so a wrapped label can never shift its input out of line with its neighbor's.
- Carbon route-dispatch contracts live in `test_carbon_route_dispatch.py`: every migrated browser route owns one `render_page` call, one private Classic callable, a callback-local import of its future Carbon renderer, the correct nav page ID, and explicit shared-state selection. It also pins the fixed Classic footer switch and real `/settings` Classic/Carbon behavior. The ignored baseline capture harness separately proves all 13 normalized response bodies byte-identical under direct Classic selection and forced Carbon import failure.
- The Statistics view (`quasarr.api.statistics.carbon.render_statistics`) contracts live in `test_carbon_statistics.py`: a 37-value coverage test with unique, non-colliding fixture numbers pins every `StatsHelper.get_stats()` field the Classic renderer displays (four top KPI metric tiles plus five detail tiles across a `.cds-grid--2` and a `.cds-grid--3`); `crypter_sweep_state` renders through the shared `status()` dot (capitalized for display, never a `cds-tag`) and `crypter_individual_mode` stays plain unbadged text in its own key/value row, falling back to lowercase `"none"` when empty; `crypter_sweep_deadline_epoch` renders as a `<time datetime=... data-epoch=...>` element that is omitted (no `data-epoch="0"`, no epoch-zero date text) when the deadline is unset; the two headings `Filecrypt cohort` and `Terminal operations` are pinned verbatim. Guard tests pin no renderer-owned emoji, no 32/64-hex identifier-shaped substrings, no `http(s)://` URLs, and no `sweep_id`/`offer_id`/`fingerprint`/`operation_id`/`package_id` labels, plus a full `carbon_templates._assert_structural_guards` pass. A route-dispatch test drives the real `/statistics` route with `QUASARR_UI=carbon` to prove the callback-local import resolves to this module. The four top KPI tiles render inside one `<div class="cds-kpi-row">` wrapper (Carbon UI integration): a dedicated test locates that wrapper and the immediately-following 2-column detail grid (`.cds-grid--2`) - `.cds-tile__content` divs inside each tile rule out a naive non-greedy `</div>` match - to prove exactly four `cds-tile--is-metric` tiles are inside it. The route's live protected-queue CAPTCHA-badge count is exercised at this integration level (success and lookup-failure-returns-zero) via the shared `carbon_templates.protected_captcha_count()` helper it now calls; that helper's own unit coverage, including the sanitized-log assertion, lives in `test_carbon_templates.py`.
- `test_hostname_rows.py` pins `storage.setup.hostnames.build_hostname_rows()`: exact row key set, source-order preservation, every status branch (unset/ok/skipped/error from a missing *arr client/error from a stored issue), the dual-category *arr requirement matrix (either client sufficient, both missing reports "Radarr or Sonarr"), the shared DJ/SJ `JUNKIES` credential section, and that rows never carry a `user`/`password` key or value. `ClassicGoldenOutputTests` proves `hostname_form_html()` is still byte-for-byte unchanged by hashing its output against SHA-256 digests captured from the pre-extraction implementation over a fixed synthetic fixture, and separately pins the positional skip-indicator placement. `HostnameApiProjectionTests` pins that `GET /api/hostnames` is registered with the same `@require_api_key` auth mode as the existing `POST /api/hostnames`, passes `audit_route_auth_modes()`, and that `get_hostnames_data()`'s projection is `build_hostname_rows()` minus `status_emoji` (Carbon markup never renders emoji; `status_emoji` stays on the row builder itself for `hostname_form_html()`'s in-process Classic consumption) with no secret ever entering the serialized JSON.
- The Carbon Downloads data contract lives in `test_carbon_downloads_contract.py`: a device-isolation service test proves `get_packages_for_device()` never calls a raising `shared_state.get_device()` fake and routes cache construction, offline/not-downloadable/file-error cleanup, archive detection, and auto-start entirely through one supplied device double, while legacy `get_packages()` still resolves its own device that way exactly once. A schema validator - self-tested against deliberately broken fixtures before being trusted - pins the exact `PackageListResponse`/`QueueRow`/`HistoryRow`/`DeferredRow` key sets, the closed `status`/`state` enums, and rejects any URL, 32/64-hex sweep/offer/fingerprint pattern, or forbidden key name (`password`, `url(s)`, `hostname`, `sweep_id`, `offer_id`, `link_fingerprint(s)`, `operation_id`, `terminal_operation_id`, `original_url`) anywhere in the serialized response. Projection behavior is driven against real protected/failed rows (including through a real `CrypterCooldownService` with an injected clock for provisional/expired/crypter-wide-cooldown holds) for `fail`-mode purity, inactive/expired defer metadata, malformed protected/failed rows, the unknown-ETA sentinel, raw (unescaped) name passthrough, and the three latest-commit Filecrypt terminal-blacklist outcomes (continuing with an alternative link, appearing once in failed history with the fixed reason text, and a scrubbed-away package producing no row at all). `DeferredRow.state`'s `probe_queued`/`retest` precedence over plain `cooldown` is unit-tested directly against `carbon._build_deferred_row()` with the exact post-projection shape `get_packages_for_device()` attaches, since reaching those states through a live cohort sweep is out of scope for this projection layer. The route test proves `GET /api/packages/list` reads `shared_state.values` once, returns the empty shape without importing the projection when no device is connected, and otherwise delegates only to `build_package_list_response()`.
- The Carbon Dashboard and Settings views (`api.carbon.render_dashboard`/`render_settings`) are covered in `test_carbon_dashboard.py` and `test_carbon_settings.py`. Both pin `build_dashboard_model`/`build_settings_model` against a fake `shared_state` whose `get_device()`/`get_packages()` raise `AssertionError`, proving neither builder ever calls them; `test_carbon_dashboard.py`'s `CarbonDashboardHeadRequestTests` additionally drives a real Bottle `HEAD /` request through the actual route with every disk-backed dependency stubbed and asserts an empty 200 body carrying the Carbon CSP header (proof the Carbon renderer, not the Classic fallback, produced it) - the concrete "HEAD never performs JD I/O" contract. Render tests pin the Dashboard's CAPTCHA banner, four `.cds-kpi-row` status tiles, the empty/loading `#dashboard-queue-tile` scaffold (never populated server-side), the API-access tile, and the all-time-summary band; Settings render tests pin all seven sections, the B1 Link Protection segmented radio plus its stored/environment/default sweep-window source tag and "Use Docker/default value" checkbox-disables-input contract, and structural/privacy guards. `CarbonSettingsJsMergeBeforeSaveTests` is the behavior-equivalent replacement for source-string coupling to Classic's pinned strings (`test_crypter_block_settings.py` continues to own those): it extracts each shipped `carbon.js` save function's body via brace-matching and asserts call order and DOM-field reach. Notifications' single `buildNotificationsPayload()` fetches `/api/notifications/settings` before building the payload and reads every rendered credential field for both providers unconditionally (`assertNotIn("provider ===", body)` - a regression pin: an earlier per-provider-scoped version read only the DOM field of the provider being saved and silently discarded an edit typed into the other provider's field, which is why `test_carbon_settings.py::CarbonSettingsRenderTests` also pins that Discord/Telegram credential fields render their real configured value into `value=`, and why the section has exactly one `notifications-save` action against two per-provider `notifications-test` actions); `mergedCaseMap()` is invoked for both providers' toggle and silent maps, cloning the fetched base before overlaying only known case keys so a case outside this page's rendered set survives for either provider; `saveTimeoutSettings()` clones the fetched base the same way before overlaying only known keys; `saveArrSettings()` fetches before falling back to the fetched `api_key` when the DOM field is blank, while `clearArrSettings()` sends an explicit empty pair with no fetch; `saveFlareSolverrSettings()` also has no fetch, since a blank URL is FlareSolverr's own existing intentional clear action. `FlareSolverrJsonBranchTests` in `test_carbon_settings.py` pins the additive JSON branch on `save_flaresolverr_url()` (success/failure/invalid-payload/clear all return `{success, message}` JSON) and proves the exact existing form-encoded branch (HTML via `render_reconnect_success`/`render_fail`) is untouched.
- The Carbon CAPTCHA view (`api.captcha.carbon.render_captcha`) lives in `test_carbon_captcha.py`. `CaptchaCarbonRenderTests` drives the real `/captcha/<provider>` routes with `?ui=carbon` (Bottle app via `setup_captcha_routes`, `carbon_assets_available` patched true) across all six providers and pins: exact escaped CTA wording (`Open <Provider> &amp; get links`) plus the `/captcha/<provider>.user.js` reference, Hide rendering through the identical shape as the other five, the quick-transfer query contract appended to the first prioritized link (including appending to an existing `?`-query with `&amp;` rather than a second `?`), Junkies' `jk_user`/`jk_pass` appended only when `Config("JUNKIES")` holds both fields, HE's `None` password coerced to `""`, the "Manual submission" tile heading plus its `cds-btn--secondary` "Submit links" button, the exact `/captcha/bypass-submit` manual-form action/method/enctype/field-name contract, the hidden `#failed-attempts-warning` with its `cds-btn--danger-ghost` Delete Package action, the source button present only with an `original_url` (worded "Open source page", `cds-btn--secondary cds-btn--cta`), the base64 package-selector option contract appearing only for 2+ protected packages (labeled "Package · N waiting", `cds-field__select cds-mono`) while a single package still renders the tile's meta line without a selector, an unsupported provider raising `ValueError`, a malformed `data` payload rendering a Carbon error notification instead of crashing, `check_package_exists()`'s 404 propagating unchanged through the Carbon path, and that the classic-only routes removed from `/captcha/*` (`cutcaptcha`, `circle`, `filecrypt/manual`) stay 404 under Carbon too. `CaptchaCarbonNarrowLayoutTests` (a sibling `TestCase` sharing `_CaptchaCarbonTestHelpers`, not a subclass of `CaptchaCarbonRenderTests` - inheriting a `TestCase` would re-run its tests under a second name) pins the target-design 720px centred column: the bare `<div class="cds-page--narrow">` wrapper, eyebrow "Link protection" plus title "Solve CAPTCHA", the release name rendered as the page-header subtitle plus the per-provider document `<title>`, the inline info notification (`cds-notification--info cds-notification--inline`), the package/actions/manual tiles wrapped in the shared `<div class="cds-stack">` (proof of the 16px gap between tiles that carry no margin of their own), the tile's `cds-meta` line (`Crypter: <strong>...</strong> · Mirror: <strong>...</strong> · Links: <strong>...</strong>` - Mirror is the live `urlsplit(url).netloc` of the payload's own first link, never a literal), both CTA buttons carrying `cds-btn--cta`, the helper line ("First time? ...") rendered as `cds-text-link` and pointed at the exact same `data-action="captcha-reset-tutorial"`/storage key as the Reset Setup Guide button - never `captcha-open`, which would silently navigate once the tutorial has already been seen instead of reopening it, the danger-ghost delete button (`cds-btn cds-btn--danger-ghost` / `type="button"` / `data-action="package-delete"` / `render_icon("trash-can")`), and that no `>Back<` text remains anywhere on the page. `JdownloaderDisconnectedCarbonTests` drives `api.jdownloader.get_jdownloader_disconnected_page()` directly (`QUASARR_UI=carbon` env, `Config` and `carbon_assets_available` patched) and pins its Carbon branch: the same narrow layout, eyebrow "JDownloader" / title "Connection required", an error notification, the device status via the shared `status()` helper (`cds-status cds-status--error`, wrapped `cds-mono`, falling back to the label "JDownloader" rather than a bare dot when `device_name` is `""`), and the "Open JDownloader settings" (primary, `href="/settings"`) / "Retry now" (tertiary, `data-action="jd-retry"`) actions - the Classic branch stays covered separately in `test_carbon_auth_pages.py::JdDisconnectedDispatchTests`, which this task's rewording keeps green (its `assertIn("Disconnected", text)` pin is satisfied by the new error notification's title, "JDownloader Disconnected"). `CaptchaCarbonJsCssStructureTests` pins the shipped `carbon.js`/`carbon.css` additions via the same brace-matching approach `test_carbon_settings.py` uses: the three `window.incrementCaptchaAttempts`/`getCaptchaAttempts`/`clearCaptchaAttempts` compatibility functions stay window-scoped; `carbon.js` still contains no `http://`/`https://` literal (the tutorial's Tampermonkey/userscript links are pre-rendered server-side into a hidden container and only read by `openProvider()`, never assembled from a URL in JS); `openProvider()` checks `tutorialSeen()` before navigating and always calls `window.incrementCaptchaAttempts()`; `onCaptchaSubmit()` increments the attempt counter without ever calling `preventDefault()`; `onCaptchaChange()` builds the `/captcha/<type>?data=<encoded>` navigation from the package-selector's `type|data` option value; `onCaptchaToggle()` swaps the manual-submission summary text; `resetTutorial()` clears storage and reopens the shared modal; `onCaptchaClick()`'s `package-delete` case navigates to the clicked element's own `data-href`; the brace-matched `bootstrapCarbonStatusPages` IIFE body (never a naive "next `(function`" search, which a nested callback like `.then(function onHeadResponse(...) {` inside `startReconnectPoll` would truncate early) wires `[data-action="jd-retry"]` to `window.location.reload()`; and `.cds-captcha-actions`/`.cds-meta` are real CSS rules in `carbon.css`.
- The Carbon auth/status pages (login, success/failure/reconnect, JD-disconnected, and generic 401/403/404 pages) live in `test_carbon_auth_pages.py`, with the two new `carbon_templates` primitives (`render_carbon_simple_page`, `render_carbon_error_page`) pinned directly in `test_carbon_templates.py`. Real Bottle-WSGI requests (the `tests/test_page_dispatch.py` house pattern, reused here as a module-level `wsgi_request()` helper) prove: `/login` GET/POST Classic-vs-Carbon rendering with `next` sanitized identically in both, POST-success cookie/redirect untouched (`_handle_login_post()` was not modified), and a Carbon-render exception in the login closure still serves the Classic login page byte-for-byte (the safety net) with no CSP header leaking through. `require_basic_auth()` keeps its exact Classic `"Authentication required"` body and `WWW-Authenticate` header in both UI modes and on Carbon-render failure; invalid-API-key 401/403 responses stay plain-text and are never Carbon-rendered. `render_error_page` is proven closed to `{401, 403, 404}` with the status code surviving Classic rendering, Carbon rendering, and Carbon-render-exception fallback alike, including the "package not found" case (a specific `message` at `status_code=404`) - this primitive's production wiring is `auth.require_basic_auth()` (with `show_classic_switch=False`) plus `back_href` now plumbed through `render_error_page` for future callers; `api/captcha/__init__.py::check_package_exists()` wires the same Carbon primitive directly (bypassing `render_error_page` since its Classic body must stay a byte-identical pre-existing literal, not `render_error_page`'s own Classic branch) - that coverage, plus the byte-parity golden test for the Classic body, lives in `tests/test_carbon_captcha.py`. An `@app.error(404)` Carbon handler for unmatched routes remains unwired: Bottle applies a registered `error_handler` to every raised `HTTPError` of that status code globally, by type alone, regardless of which route raised it - it cannot distinguish an unmatched browser path from `sponsors_helper/__init__.py`'s and `static_assets.py`'s existing `abort(404, "...")` JSON/plain-text bodies without added route-prefix filtering, so it was left for a future task with narrower, route-scoped verification rather than risking those contracts here. `render_success`/`render_success_no_wait`/`render_fail` gain a golden byte-stability test (the exact original f-string body predating Carbon dispatch, independently re-interpolated and compared) alongside their new Carbon branches (`data-action="continue-countdown"`/plain anchors, no inline `<script>`), and `render_reconnect_success`/`get_jdownloader_disconnected_page` are covered the same way (Classic byte-stability, Carbon dispatch, exception fallback). `CarbonJsStatusPageScriptTests` pins the new `carbon.js` `continue-countdown`/`reconnect-poll` actions exist and preserve the exact previous inline-script timing (1000ms ticks, `HEAD`/`no-store`, 500ms pre-reload pause). `UnauthenticatedAssetAccessTests` drives a real unauthenticated request for `/login`, extracts its own asset URLs, and re-requests them to prove CSS/JS/fonts stay publicly reachable.
- The Carbon Downloads page (`api.packages.carbon.render_downloads` and its `carbon.js`/`carbon.css` companions) is covered in `test_carbon_downloads.py`. `DownloadsSkeletonRenderTests` drives the real route through a `shared_state` double whose `get_device()` raises, proving `render_downloads()` never touches JDownloader; it pins `#downloads-search` living in the Queue tile's `.cds-tile__head-row` next to the live `Queue (n)` count, with a `cds-visually-hidden` `<label>` for its accessible name and the placeholder "Search releases"; the filter sits inside `#downloads-content`, so a companion static check pins that no render path writes `innerHTML` or replaces the container - only `<tbody>` rows are cleared, which is the whole reason a typed value and the input's focus survive a poll. It also pins the A1 deferred-table header order (selection/Release/State/Evidence/Next check/Sweep progress/unlabelled action column, matching Queue's and History's own unlabelled action columns), the bulk-toolbar buttons' `disabled` attribute at zero selection, the `deleted=1`/`deleted=0` status banner mirrored from the existing redirect route, structural guards, and the absence of any URL/32-hex/64-hex-shaped text. Because every deferred/queue/history/other row is built client-side (no server-rendered duplicate - see the api/AGENTS.md contract), the JS itself is pinned by static analysis with no JS engine, mirroring `test_deferred_packages_api.py`'s brace-matching technique: `DownloadsCountdownAndSelectionContractTests` proves exactly one shared `<el>.dataset.cohortDeadlineEpoch ?? <el>.dataset.retryAfterEpoch` read exists in the whole file, that `updateDeferredCountdowns()` delegates to it instead of resolving its own dataset read, that `upgradeEpochTimes()` (the statistics `<time data-epoch>` local-time/relative-phrase upgrade) never reads either deferred epoch attribute, and that `restoreDeferredSelection()` builds `new Set(<its one parameter>)` and guards every `checkbox.checked = true` on `selected.has(checkbox.value)` with no second snapshot read. `DownloadsD1WordingTests` pins the exact wording split: `confirmDeletePackage()` (ordinary queue/history rows, navigates to the unchanged `/packages/delete/<id>` route) says "Delete package and files" and names disk/irreversibility; `confirmRemovePending()` (the existing `DELETE /api/packages/deferred` route) says "Remove pending package(s)" and names neither; `records_only` appears nowhere. `NoDeadRetestBadgeTests` pins that the JS state-label map has no `retest` key and that an unrecognized state falls back safely instead of a dead badge. `BlacklistScrubSimulationTests` reuses test_carbon_downloads_contract.py's fixture pattern (a real `CrypterCooldownService` with an injected clock) against the actual `GET /api/packages/list` Bottle route - the same route `loadDownloads()` polls - to prove the c294f65 blacklist-scrub guarantees (alternative link survives without leaking a fingerprint; a sole-link package appears once in failed history with the fixed reason) hold for the exact JSON this page renders from. `DeferredRouteAvailabilityTests` confirms the existing probe/delete routes' `fail`-mode behavior is unchanged (probe unavailable, delete still works given real defer metadata).
- Sorting, per-table search, and the package-origin columns are covered in `test_carbon_downloads.py`: every table declares `data-table-key` and its heads `data-sort-key` + `aria-sort="none"` (nothing ships pre-sorted; carbon.js owns the live state), every sortable head wraps its label in a real button so it is reachable by keyboard, each row builder emits exactly as many cells as its own table head (the one pin tying two files' column counts together), `sortRows()` reorders the DATA array before the DOM so the 5s poll cannot undo it and selection-restore-by-value keeps working, a missing value sorts last in BOTH directions, the column that IS a table's default toggles direction instead of dead-ending on itself, a sort click re-renders from the cached payload rather than waiting on a poll that returns early while one is in flight, and `SEARCH_SCOPES` covers each of the five row bodies and four fields exactly once while `rowMatchesTerm()` matches name plus crypter plus host. `test_carbon_dashboard.py` pins the cooldown banner: absent without a cooling crypter, one line per crypter inside a SINGLE `.cds-notification__message` (the `--inline` modifier gives that class `flex: 1`, so siblings would split the banner's width instead of stacking), a hostile label escaped, a malformed deadline rendered as 0, the banner above the CAPTCHA notice, and `startCooldownCountdown()` ticking and hiding itself once spent.
- Regression coverage from a review of the Downloads page (six findings) lives in `test_carbon_downloads.py::DownloadsRegressionFixesTests`. The keyboard-unreachable ordinary-delete confirm control (an `<a>` with no `href`) is pinned by asserting the shipped `confirmDeletePackage()` control is a `<button` (never `<a`) with no leftover `preventDefault()`. The Other-packages count freezing after the first poll - because `restoreCollapseState()`'s last line assigned `summary.textContent = <string>`, destroying the `#downloads-other-count` span so `byId()` found it only once - is pinned by four static checks: `updateOtherSummary()` never does a bare `summary.textContent =` string assignment and instead rebuilds via `createTextNode`/`appendChild(countEl)`; it re-fetches `countEl` via `byId()` with a `createElement('span')` recreate-if-missing fallback; `renderOtherTables()` stores the live total in `lastOtherTotal` every call; `restoreCollapseState()` no longer reads the count back out of the summary's own previous text (`summary.textContent.match`/`countMatch` both absent). A live two-consecutive-poll (in fact seven-poll) browser check that the span survives and keeps reporting the correct total is documented separately rather than executed in this suite, which has no JS engine. The `deleted=` query param never being cleared - and a false carbon.py docstring claiming it already was - is pinned by asserting `clearDeletedQueryParamAndScheduleBannerHide()` is shipped and wired into the `DOMContentLoaded` init, and that the corrected carbon.py docstring names the real function. That A1's actions must be real controls rather than styled text pins that `carbon_icons.py`'s `ICONS` dict gained `trash-can`/`renew`/`unlocked` with a Carbon-repo `source_url` and a 64-hex `sha256`, that `buildActionButton()`/`buildCaptchaLink()` use the `cds-icon-button` class and `buildActionIcon()`, and that `carbon.css` has `.cds-row-actions{display:flex;...gap:4px;...}` and `.cds-icon-button--danger`. The two shapes are pinned apart deliberately: the queue/history delete stays icon-only (it repeats once per row in a dense table), while the deferred row's Check and Remove are labelled `cds-btn--ghost`/`cds-btn--danger-ghost` `cds-btn--compact` buttons built by `buildTextActionButton()` - so that function is pinned to carry no `cds-icon-button` class and no `buildActionIcon()` call, `buildDeferredRow()` to call no `buildActionButton()` at all, and `buttonTextForAction()` to return exactly "Check"/"Remove". That selection must be snapshotted immediately before the tbody is replaced, not before the fetch - the race window widens on a slow connection - pins that `selectedDeferredPackageIds()` is called inside `renderDeferredTable()` and no longer inside `loadDownloads()`. Label-in-name / WCAG 2.5.3 pins that every row action's `aria-label` leads with the fixed phrase (`'Check now: ' + row.name`, `'Remove pending package: ' + row.name`, `'Delete package and files: ' + row.name`, `'Solve CAPTCHA: '`) and that `title` is always set from the same `label` variable as `aria-label`, never a second independently worded string - for `buildActionButton()`, `buildTextActionButton()` and `buildCaptchaLink()` alike. For the labelled buttons the rule bites twice, since their visible word must also be a prefix of that phrase ("Check" of "Check now: …", "Remove" of "Remove pending package: …"), which is asserted directly. `DownloadsSkeletonRenderTests` gained two companion Python-side checks: the bulk toolbar buttons carry a real `<svg>` alongside their visible text, and their `aria-label` contains that visible text verbatim (the original "Check the selected..."/"Remove the selected..." wording inserted "the" and broke containment).
- `carbon.js` ships ONE shared script for every Carbon page, each as its own top-level IIFE - `test_carbon_downloads.py`'s whole-file `self.js` absence/closed-set scans for tokens this page's rows must never carry (e.g. `.hostname`, part of the packages-list forbidden-field list) broke once the Hostnames/Categories `bootstrapCarbonHostnamesAndCategories` IIFE merged in and legitimately used that same token for its own, unrelated hostname rows - a false positive, not a real leak (verified: `.hostname` never appears inside the `bootstrapCarbonDownloads` IIFE slice). Fixed with `_downloads_iife_source(js)`/`_carbon_time_and_downloads_iife_source(js)` (`tests/test_carbon_downloads.py`): both slice from an exact-once IIFE marker to end of file (carbon.js's last two IIFEs, `bootstrapCarbonTime` then `bootstrapCarbonDownloads`, with nothing after them), and every ABSENCE assertion whose token another page could plausibly also use for its own legitimate purpose (the forbidden-field list, `records_only`, the closed epoch-dataset-name set) is scoped to one of these slices instead of the whole file. Assertions that only need one specific, uniquely-named function or literal (`javascript_function_body()`, presence checks) stay on the whole file - there is no plausible second page defining an identically-named function. Two invariants stayed deliberately file-global: no remote URLs/inline handlers/http-or-xmlns literals (bind every page equally), and "exactly one shared countdown-epoch-fallback implementation in the whole shipped script" (a specific enough pattern, and a genuine claim about the whole codebase never containing a second competing implementation, that no unrelated page could plausibly trip it by coincidence). Any future IIFE added to carbon.js should default to assuming its own tokens (especially field/attribute names shaped like `.hostname`, `.password`, `.url`) could collide with another page's absence assertion the same way, and scope accordingly rather than relying on a then-empty rest-of-file.
- Carbon dispatch for all eight temporary setup servers (path, hostnames, per-source hostname credentials, FlareSolverr, *arr client selector, Radarr, Sonarr, JDownloader) lives in `test_carbon_setup_pages.py`. Direct render-function tests drive `quasarr.storage.setup.carbon.render_setup_*()` in isolation (patched `Config`/`DataBase`/`build_hostname_rows`/`get_source_metadata`) and pin: structural guards pass (a violation raises inside `render_carbon_simple_page`, so a passing call is the proof), the uncontrolled per-hostname `details` exception text never enters the initial Hostnames markup, credential inputs always render with no `value=` attribute at all, the skip banner and FlareSolverr-required warning render only when applicable, and hostile row/path input is escaped. Route-dispatch tests build each real `*_config()` Bottle app with `Server` replaced by a non-blocking `CapturingServer` (patches entered via `self._enter()`/`addCleanup`, never a `with` block that would exit - and silently un-patch module globals like `Config` - before the later WSGI request that actually triggers the route closure) and drive real `GET /` requests via `?ui=classic`/`?ui=carbon`: Classic output is proven unchanged via literal Classic-only fragments, Carbon output is a real `<!doctype html>` document with the expected `data-action`/form-`action` markers. `test_get_api_hostnames_route_exists_for_details_panel_fetch` pins the new `GET /api/hostnames` route this Carbon dispatch added to the setup app itself (previously only `POST` existed there) - a regression a live-browser smoke test caught that no WSGI-level test alone would have, since every server-side unit test passed while the route was still missing. `CarbonJsSetupFlowsScriptTests` pins the new `bootstrapCarbonSetupFlows` IIFE marker's exact-once presence, its six `case '<action>':` labels, that it sits before `bootstrapCarbonTime`/`bootstrapCarbonDownloads` (preserving the "last two IIFEs, nothing after them" invariant those tests' marker-to-EOF slicing depends on), and the absence of remote URLs inside its slice.
- Responsive/accessibility hardening coverage spans several existing files rather than adding a new one. `test_carbon_settings.py::CarbonSettingsBooleanTogglesUseSwitchComponentTests` pins that all 14 boolean Settings controls (4 timeout rows, filecrypt-enabled, sweep-window-default, 2 providers x 2 cases x enabled/silent) render through `toggle()` (`role="switch"`, exact same `id`s carbon.js's `readCheckboxValue()`/`byId()` reads depend on) with zero raw `<input type="checkbox">` left on the page; `CarbonSettingsCssContractTests` pins `.cds-checkbox-field` was fully removed (its last consumer moved to `toggle()`/the new `.cds-toggle-row` inline-group wrapper). `test_carbon_templates.py::ModalFocusChainTests` pins `showModal()` only reassigns `lastFocusedElement` while `modalElement.hidden` is true (exactly one guarded capture site) - a chained modal (a button inside an already-open modal's body opening a second dialog) must never overwrite the original opener with a soon-to-be-destroyed node; `NavOverlayFocusTrapTests` pins the new `trapNavFocus()` traps Tab inside the open compact-viewport nav the same way `trapModalFocus()` does, and is wired into the shared `onKeydown` handler. `CarbonStaticContractsTests::test_carbon_css_has_no_unclosed_rule_or_media_blocks` is a whole-file comment-aware brace-stack walk (`_css_unclosed_block_starts()`) guarding against the exact bug found and fixed here: two blocks (Hostnames/Categories' 672px media query, `.cds-status-footer .cds-classic-link`) were missing their own closing `}`, silently nesting every rule textually between them and the next real close - substring/regex checks on the CSS text cannot catch this, since the selector text is still present, merely nested somewhere it was never meant to be. `test_carbon_downloads.py::StickyCheckboxColumnTests` pins both the checkbox column (`th`/`td:first-child`) and release column (`nth-child(2)`) get `position: sticky` together inside the SAME 672px media block - anchored on the sizing rule and walking to the NEXT such block, since carbon.css ships several unrelated 672px blocks and the nearest preceding one is not necessarily the right one; `DownloadsNoscriptTests` pins the `<noscript>` warning notice (pointing at `/ui/classic?next=/packages`) precedes `#downloads-content` and survives structural guards. `test_carbon_config.py::CarbonConfigCssContractTests` pins `.cds-hostname-row__skip-banner` never sets warning yellow as a text `color:` (measured 1.5-1.7:1 in light theme before the fix) and instead uses the dark-text-on-tinted-background pattern every other warning surface in the file already uses. Live-browser verification (Playwright/chrome-devtools-mcp against rendered fixture pages, not part of this suite) additionally confirmed: the modal-chain focus fix end-to-end (Hostnames' GC row - status detail -> "Require login again?" -> Cancel returns focus to the original Details button, not `document.body`), the nav Tab-trap fix before/after, live `getComputedStyle` contrast values matching the computed WCAG ratios above in both themes, and Lighthouse accessibility=100 on Hostnames/Downloads/Settings in both themes and both desktop/mobile emulation.

## Work Guidance

- Parameterized cases use `self.subTest(...)`; many simultaneous patches use `contextlib.ExitStack` or the parenthesized multi-context `with` form.
- Tests may reach into private underscore-prefixed helpers freely.
- Document behavioral intent with comments inside tests - explain WHY a rule/ordering exists (see `test_wx_direct_links.py`).
- Prefer exact-equality assertions on whole result shapes and on recorded request sequences.

## Verification

- `uv run python -X utf8 -m unittest discover -s tests`

## Static assets tests

- `tests/test_static_assets.py` owns static route behavior coverage for MIME, `nosniff`, cache policy, traversal/suffix rejection, idempotent registration, and clean 404 behavior.
- Setup integration tests must call real `setup_auth()` and the real auth hook, and prove unauthenticated access is blocked for a protected route while `/static/...` remains public.
- Main API registration coverage keeps the `CapturingServer` approach and asserts `/static/<filename:path>` is registered and public.

## Child DOX Index

None.
